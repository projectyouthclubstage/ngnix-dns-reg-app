export class CreateDns {

    constructor(
        source: string,
        target: string,
      ) {
          this.source = source
          this.target = target
        }

    source: string;
    target: string;
}
